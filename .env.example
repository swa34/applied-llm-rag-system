# Placeholder reference for existing component examples.
# This file does not configure a runnable application. Do not use real services
# or private documents: ingestion dry-run and cloud sharing need fixes first.
# Keep provider credentials server-side. Empty values are deliberate.

OPENAI_API_KEY=
EMBED_MODEL=text-embedding-3-large
PINECONE_API_KEY=
PINECONE_INDEX_NAME=fictional-demo-placeholder
PINECONE_NAMESPACE=fictional-demo-placeholder

REDIS_URL=redis://localhost:6379
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=fictional_demo_placeholder
DB_USERNAME=demo_placeholder
DB_PASSWORD=
# Existing TLS verification behavior needs review before remote DB use.
DB_SSL=false

# Cloud integration is optional architectural material, not a demo requirement.
DROPBOX_ACCESS_TOKEN=
